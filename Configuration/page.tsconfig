TCEMAIN {
    table {
        tt_content {
            # pages copied will be enabled by default
            disableHideAtCopy = 1

            # pages copied will not have (copy 1) appended
            disablePrependAtCopy = 1
        }
    }
}